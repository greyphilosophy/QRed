# QRed — Frontend build and test dependencies
# All PDF sealing happens in-browser (pdf-lib + jsQR).
# No backend runtime dependencies are required.

# Testing
pytest>=8.0
playwright>=1.40
reportlab>=4.0