{
  "name": "qred-cloudflare-pages",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build:pages",
    "build:worker": "cd frontend && npm ci && npm run build:pages"
  }
}
