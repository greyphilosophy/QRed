{
  "name": "qred-cloudflare-pages",
  "private": true,
  "version": "0.1.0",
  "description": "Root shim so Cloudflare Pages builds correctly when repo root is used",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build:pages",
    "build:worker": "cd frontend && npm ci && npm run build:pages"
  },
  "dependencies": {
    "@noble/ed25519": "^3.1.0"
  }
}
